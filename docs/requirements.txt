# Python dependencies for building the docs site (mkdocs).
# `make docs-deps` installs these into a repo-local .venv (see the Makefile docs block).
# Unpinned for now; pin exact versions with `.venv/bin/pip freeze | grep -iE 'mkdocs|swagger'`
# once a local `make docs-build` is green, if you want fully reproducible builds.
mkdocs
mkdocs-material
mkdocs-swagger-ui-tag
